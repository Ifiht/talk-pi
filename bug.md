# Bug note

Fix in place:

- `tools/piper/` auto-download on missing
- `tools/sox/` auto-download on missing
- `tools/whisper/models/ggml-base.bin` auto-download on missing

Talk-Pi now shows `Talk-pi: Downloading 📥` during bootstrap.
